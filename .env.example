# .env로 복사해서 실제 값 채울 것 (.env는 git에 커밋되지 않음)
# MEALC_USER_ID/PASSWORD는 test/ 아래 로컬 테스트 스크립트에서만 쓰인다.
# 배포된 시스템(DynamoDB)은 이 파일을 쓰지 않는다 — 회원가입 API로 입력한 값이 암호화되어 저장된다.
MEALC_USER_ID=YOUR_USER_ID
MEALC_PASSWORD=YOUR_PASSWORD
DATA_GO_KR_API_KEY=YOUR_DATA_GO_KR_SERVICE_KEY
